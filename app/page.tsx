export const metadata = {
  title: "App Router",
};

export default function Page() {
  return (
    <h1>
      {/* <button
        type="button"
        onClick={() => {
          throw new Error("Sentry Frontend Error");
        }}
      >
        Throw error
      </button> */}
    </h1>
  );
}
